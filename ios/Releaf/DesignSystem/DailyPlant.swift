// Moved to `DailyPlants.generated.swift`.
//
// The DailyPlant type and the DailyPlants enum (with the 31-entry
// rotation list and forToday() lookup) are now emitted from
// `design-system/design-tokens.json` by the token generator.
//
// This file is kept on disk as a near-empty stub because the build
// sandbox in some environments doesn't allow deletion of source
// files. It contributes no compiled code. Safe to delete from a
// developer machine when convenient.
