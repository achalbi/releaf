/*
 * Placeholder.swift
 *
 * SwiftPM rejects targets with no source files. This file exists only
 * so `ReleafCoreData` resolves while the target is empty.
 *
 * Real contents arrive in PR #4 of the QuickInk spinoff plan
 * (see docs/QUICKINK_PROPOSAL.md §2). The target will hold:
 *   - Uuidv7
 *   - IsoClock
 *   - FtsQuery
 *   - AttachmentStorage (parameterized for app folder name)
 *
 * Delete this file in the same PR that lands the real sources.
 */

import Foundation

@available(*, unavailable, message: "ReleafCoreData target is in skeleton state — see PR #4")
internal enum _ReleafCoreDataPlaceholder {}
