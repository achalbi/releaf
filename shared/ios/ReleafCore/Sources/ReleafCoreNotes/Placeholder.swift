/*
 * Placeholder.swift
 *
 * SwiftPM rejects targets with no source files. This file exists only
 * so `ReleafCoreNotes` resolves while the target is empty.
 *
 * Real contents arrive in PR #4 of the QuickInk spinoff plan. The
 * target will hold:
 *   - NotepadRepository (notepad_entries CRUD + FTS5 search)
 *   - NotepadListViewModel (UI-agnostic list VM with debounced search)
 *   - NotepadEditorViewModel (CommonMark round-trip editor VM)
 *   - The editor sheet + RichTextEditor + format bar
 *
 * Delete this file in the same PR that lands the real sources.
 */

import Foundation

@available(*, unavailable, message: "ReleafCoreNotes target is in skeleton state — see PR #4")
internal enum _ReleafCoreNotesPlaceholder {}
