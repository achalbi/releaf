/*
 * Placeholder.swift
 *
 * SwiftPM rejects targets with no source files. This file exists only
 * so `ReleafCoreDesignSystem` resolves while the target is empty.
 *
 * Real contents arrive in PR #4 of the QuickInk spinoff plan. The
 * target will hold the generated AppColors / AppTypography /
 * AppSpacing / AppShadow / AppRadius / AccentPalette files plus the
 * shared component library (Card, AppButton, DotGridBackground,
 * editor toolbar, etc.).
 *
 * Delete this file in the same PR that lands the real sources.
 */

import Foundation

@available(*, unavailable, message: "ReleafCoreDesignSystem target is in skeleton state — see PR #4")
internal enum _ReleafCoreDesignSystemPlaceholder {}
