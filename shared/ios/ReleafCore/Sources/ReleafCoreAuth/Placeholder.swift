/*
 * Placeholder.swift
 *
 * SwiftPM rejects targets with no source files. This file exists only
 * so `ReleafCoreAuth` resolves while the target is empty.
 *
 * Real contents arrive in PR #4 of the QuickInk spinoff plan. The
 * target will hold:
 *   - GoogleAuthClient (protocol + RealGoogleAuthClient + StubGoogleAuthClient)
 *   - AuthStore
 *   - KeychainTokenStore
 *   - GoogleSignInBinding
 *
 * Delete this file in the same PR that lands the real sources.
 */

import Foundation

@available(*, unavailable, message: "ReleafCoreAuth target is in skeleton state — see PR #4")
internal enum _ReleafCoreAuthPlaceholder {}
